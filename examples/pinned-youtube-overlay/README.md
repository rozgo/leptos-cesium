# Pinned YouTube Overlay

Shows an official YouTube iframe visually pinned to a Cesium globe position
using `YouTubeOverlay`.

This example demonstrates the HTML overlay path, not a Cesium material
texture. The player stays aligned to a world position while the camera moves,
but the video remains an iframe layered above the canvas.

Run:

```bash
trunk serve --open
```
