# Pinned Image Overlay

Shows a native HTML `<img>` visually pinned to a Cesium globe position
using `ImageOverlay`.

This example demonstrates the DOM overlay path, not a Cesium billboard.
The image stays aligned to a world position while the camera moves, but the
browser still owns image loading and rendering.

Run:

```bash
trunk serve --open
```
